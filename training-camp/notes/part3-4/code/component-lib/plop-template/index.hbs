import Vue from 'vue'
import Jc{{ properCase name }} from './src/{{ name }}.vue'

Jc{{ properCase name }}.install = () => {
    Vue.component(Jc{{ properCase name }}.name, Jc{{ properCase name }})
}

export default Jc{{ properCase name }}
